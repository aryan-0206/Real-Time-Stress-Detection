# Requirements for Real-Time Stress Detection (Web App)
# NOTE: opencv-python-headless is installed FIRST in Dockerfile before this list
# to prevent mediapipe from pulling in the GUI version that needs libGL.

flask==3.0.3
flask-cors==4.0.1
gunicorn==22.0.0
opencv-python-headless>=4.5.0
mediapipe>=0.10.14
numpy>=1.22.0
Pillow>=10.4.0
